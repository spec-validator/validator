import { Route } from '@validator/rest-api-server/'
import { ServerConfig } from '@validator/rest-api-server/'

const createOpenApiRoute = (
  config: Partial<ServerConfig>,
  routes: Route[],
) => {

}
